apiVersion: v1
kind: Service
metadata:
  name: inventory-api
  labels:
    app: inventory-api
spec:
  selector:
    app: inventory-api
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP        # Exposed via Ingress, not directly
