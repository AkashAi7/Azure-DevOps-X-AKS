apiVersion: v1
kind: Service
metadata:
  name: workshop-sample-app
  namespace: workshop
spec:
  type: ClusterIP
  selector:
    app: workshop-sample-app
  ports:
    - port: 80
      targetPort: 8080